[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "clearcrest"
version = "0.1.0"
description = "Experimental ClearCrest protocol reference"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["clearcrest*"]

